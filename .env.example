# Chat: GitHub Actions uses encrypted secret OPENROUTER_API_KEY (Settings → Secrets).
# Locally you may set VITE_OPENROUTER_API_KEY in .env.local — never commit that file.
VITE_CHAT_ENDPOINT=
VITE_OPENROUTER_API_KEY=
VITE_FORM_ENDPOINT=
VITE_FORM_ACCESS_KEY=
